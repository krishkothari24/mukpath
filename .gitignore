# Real credentials. `.env.example` is the committed template.
.env
.env.local

# Virtualenv for the Python bootstrapping tools (scripts/setup.sh).
.venv/
venv/

# Telethon auth session — this is a live credential for your Telegram
# account. Never commit it.
*.session
*.session-journal

# Raw scraper output and downloaded media. Bulky, regenerable, and per
# CLAUDE.md must stay out of the shipped app bundle.
mukhpath_dump*.json
mukhpath_webapp.json
downloads/
seed/audio/

__pycache__/
*.pyc
.DS_Store

# Phase 1: backend (Node)
node_modules/
